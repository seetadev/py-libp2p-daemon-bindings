#!/usr/bin/env python
# -*- coding: utf-8 -*-

import setuptools

# This file is now only needed for backward compatibility with older
# versions of pip and to support editable installs. All package
# configuration is now in `pyproject.toml`.
if __name__ == "__main__":
    setuptools.setup()
